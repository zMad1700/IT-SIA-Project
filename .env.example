VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Optional: offline demo admin credentials (not needed when using Supabase Auth).
# Generate a SHA-256 hash of your admin password and set it here.
# Example: echo -n "yourpassword" | sha256sum
VITE_ADMIN_EMAIL=admin@scholarhub.local
VITE_ADMIN_PASSWORD_HASH=
